spring:
  main:
    web-application-type: none
  application:
    name: insurance-workers
  temporal:
    connection:
      target: ${TEMPORAL_TARGET:localhost:7233}
    namespace: ${TEMPORAL_NAMESPACE:default}
    workers-auto-discovery:
      packages:
        - com.ziggy.insurance.domains
    # Capacity applied to every worker so each task queue can run up to 1000
    # concurrent workflow tasks and 1000 concurrent activity tasks. Poller counts
    # are raised above the SDK defaults (2 workflow / 5 activity) so poll
    # throughput can keep the executor slots saturated at this concurrency.
    workers:
      - task-queue: policy-task-queue
        capacity:
          max-concurrent-workflow-task-executors: 500
          max-concurrent-activity-executors: 500
          max-concurrent-local-activity-executors: 500
          max-concurrent-workflow-task-pollers: 20
          max-concurrent-activity-task-pollers: 20
      - task-queue: claim-task-queue
        capacity:
          max-concurrent-workflow-task-executors: 500
          max-concurrent-activity-executors: 500
          max-concurrent-local-activity-executors: 500
          max-concurrent-workflow-task-pollers: 20
          max-concurrent-activity-task-pollers: 20
      - task-queue: notifications-task-queue
        capacity:
          max-concurrent-workflow-task-executors: 500
          max-concurrent-activity-executors: 500
          max-concurrent-local-activity-executors: 500
          max-concurrent-nexus-task-executors: 500
          max-concurrent-workflow-task-pollers: 20
          max-concurrent-activity-task-pollers: 20
          max-concurrent-nexus-task-pollers: 20
      - task-queue: payment-task-queue
        capacity:
          max-concurrent-workflow-task-executors: 500
          max-concurrent-activity-executors: 500
          max-concurrent-local-activity-executors: 500
          max-concurrent-nexus-task-executors: 500
          max-concurrent-workflow-task-pollers: 20
          max-concurrent-activity-task-pollers: 20
          max-concurrent-nexus-task-pollers: 20
