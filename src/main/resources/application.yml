spring:
  application:
    name: temporal-insurance-demo
  temporal:
    connection:
      target: ${TEMPORAL_TARGET:localhost:7233}
    namespace: ${TEMPORAL_NAMESPACE:default}
    workers-auto-discovery:
      packages:
        - com.example.insurance
    workers:
      - task-queue: policy-task-queue
